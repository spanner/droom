.body{:style => "font-size: #{"%.2f" % scrap.text_size}em"}
  = simple_format(scrap.body).html_safe
  %p.attribution
    = scrap.note

.caption
  %p.attribution
    = render "droom/scraps/credit", :scrap => scrap
