#scraps.holder{:data => {:refreshable => true, :url => scraps_path(:format => :js)}}
  = render :partial => "thumb", :collection => @scraps

  .pagination
    = paginate @scraps, :params => {:format => nil}
