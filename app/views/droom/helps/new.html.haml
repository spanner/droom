#help
  .header{data: {draggable: ".popup", remembered: "help"}}
    = link_to "X", '#', :class => "closer", :data => {:action => "close"}
    %h2.titlebar
      = t "helps.new"

  = render :partial => 'form'
