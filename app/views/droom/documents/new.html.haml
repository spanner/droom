#document
  .header{data: {draggable: ".popup", remembered: "popup"}}
    = link_to "X", '#', :class => "closer", :data => {:action => "close"}
    %h2
      = t :add_document
      
  = render :partial => 'form'