#document
  .header
    = link_to "X", '#', :class => "closer", :data => {:action => "close"}
    %h2
      = t :add_document_to
      %strong
        = @folder.name
      = t :folder
      
  = render :partial => 'form'