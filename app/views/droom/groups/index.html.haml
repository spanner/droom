#groups
  - if current_user.admin?
    %p.admin
      = link_to t(:add_group), droom.new_group_url, :class => "add", :remote => true, :data => {:action => "append_form", :type => "html", :affected => "table.group"}
  = render :partial => 'groups'
