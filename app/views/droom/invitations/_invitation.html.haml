- invitation ||= @invitation

%li.invitation{:class => invitation.status, :id => "invitation_#{invitation.id}"}
  = link_to invitation.event.name, droom.event_url(invitation.event)
  %span.action
    - unless invitation.accepted? 
      = link_to t(:accept), accept_event_invitation_url(invitation.event, invitation), :class => 'accept', :data => {:action => "replace", :method => "put", :replaced => "#invitation_#{invitation.id}"}
    - unless invitation.rejected? 
      = link_to t(:reject), reject_event_invitation_url(invitation.event, invitation), :class => 'reject', :data => {:action => "replace", :method => "put", :replaced => "#invitation_#{invitation.id}"}
