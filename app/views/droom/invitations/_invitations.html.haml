- invitations ||= false
- if invitations || current_user
  - invitations ||= current_user.invitations.future
  - if invitations && invitations.any?
    %h2.section
      = t :your_invitations
    %ul.invitations
      = render :partial => "droom/invitations/invitation", :collection => current_user.invitations.future