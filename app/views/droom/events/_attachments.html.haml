- event ||= @event
- folder ||= event.folder

.col
  %h3
    = t :attachments
  - if folder.populated?
    = render :partial => "droom/folders/contents", :locals => {:folder => folder}
  - else
    %p
      = t :no_attachments
