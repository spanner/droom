- @seen_events = {}
- @event_header = month_header_for(Date.today)

#events
  - if admin?
    %p.admin
      = link_to t(:add_event), new_event_url, :class => "append", :remote => true, :data => {:type => "html"}

  - if user_signed_in? && current_user.person
    - if @my_future_events.any?
      %section.personal
        %p.twister
          = link_to t(:your_future_events), '#'
        .twisted
          = render :partial => 'event', :collection => @my_future_events

  - if @my_past_events.any?
    %section.past
      %p.twister
        = link_to t(:your_past_events), '#'
      .twisted
        = render :partial => 'event', :collection => @my_past_events

  - unless @my_future_events.any? or @my_past_events.any?
    %p
      = t :no_events_for_you

#marginalia
  #minicalendar
    .scroller
      = render 'minicalendar'

  = render :partial => 'droom/shared/search_form'

  #future_events.search_results
    %h4
      = t :future_events
    - if @all_events.any?
      = render :partial => 'event_line', :collection => @events
