- if current_user.admin?
  %p.admin
    = link_to t(:add_event), droom.new_event_url, :class => "add", :remote => true, :data => {:action => "append_form", :type => "html", :affected => ".events_list, .minimonth"}

#events
  %h2.quiet
    = @pagetitle
  
  = render :partial => 'events'

= render :partial => 'droom/dashboard/marginalia'
