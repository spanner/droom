#events
  - if current_user.admin?
    %p.admin
      = link_to t(:add_event), droom.new_event_url, :class => "add", :remote => true, :data => {:action => "append_form", :type => "html"}

  %h2.quiet
    = @pagetitle
    
  = render :partial => 'event', :collection => @events

= render :partial => 'droom/dashboard/marginalia'
