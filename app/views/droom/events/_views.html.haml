%span.calendar_views
  %br
  = t('event_page.stay_up_to_date')
  = link_to(t('event_page.rss'), droom.events_url(calendar_parameters.merge(:format => :rss)))
  = t('event_page.or')
  = link_to(t('event_page.calendar_feed'), droom.events_url(calendar_parameters.merge(:format => :ics, :protocol => 'webcal://', :only_path => false)))

