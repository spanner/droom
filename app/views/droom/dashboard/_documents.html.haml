- documents = Droom::Document.visible_to(current_user).latest(5)

%h2.section
  = link_to t(:latest_documents), droom.documents_url

- if documents.any?
  = render :partial => "droom/documents/document", :collection => documents, :locals => {:closed => true}
- else
  = t :no_documents