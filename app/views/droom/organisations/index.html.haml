- content_for :title do
  = t :directory

- content_for :standfirst do
  = t :organisations_introduction

- content_for :margin do
  #finder
    %h3
      = t :find_people

    %form.filter{:action => droom.users_url(:format => :js), :data => {:action => "filter"}}
      %p
        = text_field_tag :q, params[:q], :placeholder => t(:by_name_or_email)

    .scroller
      #found
  


#main.organisations
  = render :partial => 'droom/organisations/organisations'
