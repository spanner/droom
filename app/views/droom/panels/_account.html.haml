#account.panel
  %h4
    = t :account_preferences
  
  %ul
    - if current_user.person
      %li
        = link_to t(:edit_your_profile), droom.edit_person_url(current_user.person), :data => {:action => "popup", :affected => ".people_list"}
