= form_for @folder, :html => {:class => 'edit folder'} do |f|
  = f.hidden_field :parent_id
  
  %p.slug
    %strong<
      - if @folder.parent
        = @folder.parent.path + "/"
      - else
        \/
      %br
    = f.text_field :slug, :required => true, :class => "slug", :placeholder => t(:"activerecord.attributes.droom.folder.slug")
    %br
    %span.formnote
      = t :slug_note
  .buttons
    = f.submit t(:save_folder)
    = t :or
    = link_to t(:cancel), '#', :class => 'cancel'
