%li.folder.holder
  = link_to folder.slug, "#", :class => "folder"
  %ul.filing
    = render :partial => 'droom/folders/folder', :collection => folder.children
    = render :partial => 'droom/documents/listing', :collection => folder.documents
